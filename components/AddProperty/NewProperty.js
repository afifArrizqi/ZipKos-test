import React from "react";
import { StyleSheet, Text, View } from "react-native";

const NewProperty = () => {
  return (
    <View>
      <Text>This is New Property</Text>
    </View>
  );
};

export default NewProperty;

const styles = StyleSheet.create({});
